# Stock Sentiment & Technical Analysis

Predicting price moves using news sentiment and technical indicators.

## Setup

`bash
python -m venv venv
source venv/bin/activate   # or venv\Scripts\activate on Windows
pip install -r requirements.txt
python -m textblob.download_corpora
